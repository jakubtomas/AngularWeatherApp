import { ToCelsiumPipe } from './to-celsium.pipe';

describe('ToCelsiumPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCelsiumPipe();
    expect(pipe).toBeTruthy();
  });
});
